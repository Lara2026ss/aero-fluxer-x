{
  "name": "files",
  "description": "Operaciones superiores de archivos: lectura paginada, escritura atómica con backups, edición quirúrgica por líneas, gestor JSON dot-notation, CSV, documentos Office/PDF y compresión universal.\nAcciones y argumentos (usar en args): append_to_file { path, content, addNewline } | batch_copy { files, overwrite, stopOnError } | batch_delete { paths, stopOnError } | batch_move { files, stopOnError } | batch_rename { directory, pattern, replacement } | calculate_checksum { path, algorithm } | compare_files { left, right } | compress_path { source, destination } | copy_file { source, destination, overwrite } | create_directory { path } | create_document { path, title, content, paragraphs, format, frontmatter, metadata } | create_file { path, content, overwrite, backup, encoding, mode, patch } | delete { path, recursive, force } | delete_file { path, recursive, force } | delete_lines { path, startLine, endLine, lineNumbers, backup } | delete_path { path, recursive, force } | directory_tree { path, depth, maxEntries } | edit_file { path, oldText, find, search, targetContent, newText, replace, replacement, replacementContent, replaceAll, isRegex, backup } | extract_archive { source, destination } | file_diff { left, right } | file_exists { path } | find_and_replace_in_files { directory, filePattern, pattern, searchText, replaceText, find, replace, search, oldText, newText, excludePatterns, git, dist, build } | get_detailed_metadata { path } | get_file_info { path } | get_info { path } | get_metadata { path } | grep_files { path, query, isRegex, caseInsensitive, include, includePattern, limit, maxFileSize } | insert_lines { path, atLine, afterLine, afterPattern, lines, backup } | json_manager { path, op, key, value, createPath, backup } | list_allowed_directories {} | list_archive_contents { path } | list_directory { path, limit, sortBy, recursive, compact } | list_directory_with_sizes { path, limit, sortBy, compact } | list_files { path, limit, sortBy, recursive, compact } | move_file { source, destination, overwrite } | patch_file { path, searchBlock, replaceBlock, backup } | read_binary_file { path, format, limitBytes } | read_csv { path, delimiter, hasHeader, maxRows } | read_document { path } | read_file { path, head, tail, maxLines, page, linesPerPage, startLine, endLine, range, encoding, includeLineNumbers, compact } | read_file_range { path, startLine, endLine, range, encoding, includeLineNumbers, compact } | read_json { path, key, defaultValue } | read_multiple_files { paths, path, maxFiles } | read_text_file { path, head, tail, maxLines, page, linesPerPage, startLine, endLine, range, encoding, includeLineNumbers, compact } | replace_file_content { path, oldText, find, search, targetContent, newText, replace, replacement, replacementContent, replaceAll, isRegex, backup } | replace_in_file { path, oldText, find, search, targetContent, newText, replace, replacement, replacementContent, replaceAll, isRegex, backup } | replace_lines { path, startLine, endLine, lines, backup } | search_files { path, pattern, excludePatterns, git, dist, build, limit } | set_attributes { path, readonly, hidden, mode } | str_replace { path, oldText, find, search, targetContent, newText, replace, replacement, replacementContent, replaceAll, isRegex, backup } | surgical_edit { path, oldText, find, search, targetContent, newText, replace, replacement, replacementContent, replaceAll, isRegex, backup } | touch_file { path } | validate_path { path } | validate_workspace { path } | write_csv { path, headers, rows, delimiter, append } | write_file { path, content, overwrite, backup, encoding, mode, patch } | write_json { path, data }",
  "parameters": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "enum": [
          "append_to_file",
          "batch_copy",
          "batch_delete",
          "batch_move",
          "batch_rename",
          "calculate_checksum",
          "compare_files",
          "compress_path",
          "copy_file",
          "create_directory",
          "create_document",
          "create_file",
          "delete",
          "delete_file",
          "delete_lines",
          "delete_path",
          "directory_tree",
          "edit_file",
          "extract_archive",
          "file_diff",
          "file_exists",
          "find_and_replace_in_files",
          "get_detailed_metadata",
          "get_file_info",
          "get_info",
          "get_metadata",
          "grep_files",
          "insert_lines",
          "json_manager",
          "list_allowed_directories",
          "list_archive_contents",
          "list_directory",
          "list_directory_with_sizes",
          "list_files",
          "move_file",
          "patch_file",
          "read_binary_file",
          "read_csv",
          "read_document",
          "read_file",
          "read_file_range",
          "read_json",
          "read_multiple_files",
          "read_text_file",
          "replace_file_content",
          "replace_in_file",
          "replace_lines",
          "search_files",
          "set_attributes",
          "str_replace",
          "surgical_edit",
          "touch_file",
          "validate_path",
          "validate_workspace",
          "write_csv",
          "write_file",
          "write_json"
        ],
        "description": "Acción a ejecutar en el dominio files"
      },
      "args": {
        "type": "object",
        "description": "Argumentos específicos de la acción"
      }
    },
    "required": [
      "action"
    ]
  }
}