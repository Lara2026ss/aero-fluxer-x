{
  "name": "developer",
  "description": "Detección, análisis, tests, builds, gestión de skills, feedback público (Render/Firebase) y actualización.\nAcciones y argumentos (usar en args): check_update { repoRoot } | create_skill { name, description, instructions, path, rules, examples, references, scripts, overwrite } | detect_project { path } | diagnose_service { path, serviceId } | feedback_guide {} | get_skill { name, path } | inspect_project { path } | list_skills { path, searchGlobal } | refresh_service_state { path, serviceId } | run_project_build { path, timeoutMs } | run_project_tests { path, timeoutMs } | submit_feedback { type, title, description, steps_to_reproduce, expected_behavior, actual_behavior, severity, screenshot, attach_logs, tool } | update { allowDowngrade } | update_info { repoRoot } | validate_skill { path }",
  "inputSchema": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "enum": [
          "check_update",
          "create_skill",
          "detect_project",
          "diagnose_service",
          "feedback_guide",
          "get_skill",
          "inspect_project",
          "list_skills",
          "refresh_service_state",
          "run_project_build",
          "run_project_tests",
          "submit_feedback",
          "update",
          "update_info",
          "validate_skill"
        ],
        "description": "Acción a ejecutar en el dominio developer"
      },
      "args": {
        "type": "object",
        "description": "Argumentos específicos de la acción"
      }
    },
    "required": [
      "action"
    ],
    "additionalProperties": false
  }
}