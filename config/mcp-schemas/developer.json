{
  "name": "developer",
  "description": "Detección, análisis, tests, builds, gestión de skills, feedback público (Render/Firebase) y actualización.\nAcciones y argumentos (usar en args): create_skill { name, description, instructions, path, rules, examples, references, scripts, overwrite, compact, compact_mode } | delete_feedback { id } | delete_skill { name, path, compact, compact_mode } | detect_project { path } | diagnose_service { path, serviceId } | edit_skill { name, path, description, instructions, rules, examples, references, scripts, compact, compact_mode } | feedback_guide {} | feedback_outbox_status {} | get_skill { name, path, compact, compact_mode } | git_diff_summary { staged, path, revealPath, allow_user_path } | git_log_compact { maxCount, path, revealPath, allow_user_path } | git_status_structured { path, revealPath, allow_user_path } | git_switch_identity { account, name, email, path, revealPath, allow_user_path } | inspect_project { path } | list_feedbacks { type, severity, status, limit } | list_my_feedbacks { id, title, created_at, status, resolution_notes, fixed_in_version, cached_at } | list_skills { path, searchGlobal, compact, compact_mode } | read_feedback { id } | refresh_service_state { path, serviceId } | run_project_build { path, timeoutMs } | run_project_tests { path, timeoutMs } | submit_feedback { id, title, 200, type, severity, created_at, status, resolution_notes, fixed_in_version, cached_at } | upd { force, confirm, confirmed, user_confirmed, dry_run, dryRun, action, backup_id, backupId } | upd_backups { backupId, createdAt, version } | upd_check { repoRoot, force, checkRepo, revealPath, allow_user_path } | upd_data { id, path, created_at } | upd_info { version, full_history, include_full_history } | upd_rollback { backup_id, backupId, confirm, confirmed, user_confirmed } | validate_skill { path, compact, compact_mode } | verify_html_integrity {}",
  "inputSchema": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "enum": [
          "create_skill",
          "delete_feedback",
          "delete_skill",
          "detect_project",
          "diagnose_service",
          "edit_skill",
          "feedback_guide",
          "feedback_outbox_status",
          "get_skill",
          "git_diff_summary",
          "git_log_compact",
          "git_status_structured",
          "git_switch_identity",
          "inspect_project",
          "list_feedbacks",
          "list_my_feedbacks",
          "list_skills",
          "read_feedback",
          "refresh_service_state",
          "run_project_build",
          "run_project_tests",
          "submit_feedback",
          "upd",
          "upd_backups",
          "upd_check",
          "upd_data",
          "upd_info",
          "upd_rollback",
          "validate_skill",
          "verify_html_integrity"
        ],
        "description": "Acción a ejecutar en el dominio developer"
      },
      "args": {
        "type": "object",
        "description": "Argumentos específicos de la acción"
      }
    },
    "required": [
      "action"
    ],
    "additionalProperties": false
  }
}