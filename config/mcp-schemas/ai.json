{
  "name": "ai",
  "description": "Interacciones avanzadas con Aether-10 (FLONNET Multi-Provider Engine) y subagentes.\nAcciones y argumentos (usar en args): ask { prompt, message, content, task, query, messages, args } | ask_flonnet { prompt, message, content, task, query, ask, input, text, messages, args } | chat { prompt, message, content, task, query, messages, args } | count_tokens { text } | run_agent { prompt, task, maxSteps, args } | run_multi_step { prompt, task, maxSteps, args } | run_subagent { prompt, task, maxSteps, args } | status {} | summarize { content, message, prompt, messages, args } | think { prompt, message, content, task, query, args }",
  "inputSchema": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "enum": [
          "ask",
          "ask_flonnet",
          "chat",
          "count_tokens",
          "run_agent",
          "run_multi_step",
          "run_subagent",
          "status",
          "summarize",
          "think"
        ],
        "description": "Acción a ejecutar en el dominio ai"
      },
      "args": {
        "type": "object",
        "description": "Argumentos específicos de la acción"
      }
    },
    "required": [
      "action"
    ],
    "additionalProperties": false
  }
}