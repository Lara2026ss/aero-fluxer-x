{
  "name": "diagnostics",
  "description": "Diagnóstico avanzado del sistema, benchmarks, salud del MCP y auto-evaluación.\nAcciones y argumentos (usar en args): benchmark { loops } | compact_status { anonymize } | health_check { expose_host_info, compact, anonymize } | resolve_toolchain { forceRefresh } | self_test { name, value, status } | system_diagnose { level, type, message } | telemetry {} | verify_html_integrity { path, content, counts, selectors, baselineCounts }",
  "inputSchema": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "enum": [
          "benchmark",
          "compact_status",
          "health_check",
          "resolve_toolchain",
          "self_test",
          "system_diagnose",
          "telemetry",
          "verify_html_integrity"
        ],
        "description": "Acción a ejecutar en el dominio diagnostics"
      },
      "args": {
        "type": "object",
        "description": "Argumentos específicos de la acción"
      }
    },
    "required": [
      "action"
    ],
    "additionalProperties": false
  }
}