{
  "name": "guide",
  "description": "Documentación oficial interna, reglas y mejores prácticas para IAs que operan este MCP.\nAcciones y argumentos (usar en args): best_practices {} | permissions_info { level, durationMinutes } | search_tools { query, domain, limit } | tool_usage { tool_name }",
  "inputSchema": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "enum": [
          "best_practices",
          "permissions_info",
          "search_tools",
          "tool_usage"
        ],
        "description": "Acción a ejecutar en el dominio guide"
      },
      "args": {
        "type": "object",
        "description": "Argumentos específicos de la acción"
      }
    },
    "required": [
      "action"
    ],
    "additionalProperties": false
  }
}